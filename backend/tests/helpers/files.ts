import path from "path";

export const avatarPath = path.join(
  process.cwd(),
  "tests",
  "fixtures",
  "avatar.png"
);

export const resumePath = path.join(
  process.cwd(),
  "tests",
  "fixtures",
  "resume.pdf"
);